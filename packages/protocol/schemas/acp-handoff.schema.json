{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-control-plane.local/schemas/acp-handoff.schema.json",
  "title": "ACP Handoff",
  "type": "object",
  "required": [
    "handoff_id",
    "task_id",
    "from_actor",
    "to_actor",
    "completed_work",
    "remaining_work",
    "evidence_refs",
    "risks"
  ],
  "properties": {
    "handoff_id": { "type": "string", "minLength": 1 },
    "task_id": { "type": "string", "minLength": 1 },
    "from_actor": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": { "type": "string", "enum": ["human", "hermes", "agent"] },
        "agent_profile_id": { "type": "string", "maxLength": 2048 }
      },
      "additionalProperties": false
    },
    "to_actor": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": { "type": "string", "enum": ["human", "hermes", "agent"] },
        "agent_profile_id": { "type": "string", "maxLength": 2048 }
      },
      "additionalProperties": false
    },
    "completed_work": {
      "type": "array",
      "maxItems": 500,
      "items": { "type": "string", "maxLength": 8000 }
    },
    "remaining_work": {
      "type": "array",
      "maxItems": 500,
      "items": { "type": "string", "maxLength": 8000 }
    },
    "evidence_refs": {
      "type": "array",
      "maxItems": 2000,
      "items": { "type": "string", "maxLength": 2048 }
    },
    "risks": {
      "type": "array",
      "maxItems": 200,
      "items": { "type": "string", "maxLength": 8000 }
    }
  },
  "additionalProperties": false
}
