# @acp/web

**React + Vite** console scaffold: tasks, agents, runs, gates (per protocol doc).

## Dev

```bash
cd packages/web
npm install
npm run dev
```

Proxy: `/api` → `http://127.0.0.1:3840` (see `vite.config.ts`).
