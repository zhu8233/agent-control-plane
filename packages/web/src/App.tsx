export function App() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '2rem' }}>
      <h1>Agent Control Plane</h1>
      <p>Web console scaffold. Connect to <code>@acp/server</code> when API is ready.</p>
    </main>
  );
}
